import React from "react";
import { Audio } from "react-loader-spinner";

export const Loading = () => (
  <div className="flex justify-center items-center ">
    <Audio color="#00BFFF" height={550} width={80} />
  </div>
);
